version: "3.8"

services:
  metaclaw:
    build: .
    ports:
      - "${METACLAW_PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - ./metaclaw.toml:/app/metaclaw.toml:ro
      - metaclaw-data:/root/.metaclaw
    restart: unless-stopped

volumes:
  metaclaw-data:
